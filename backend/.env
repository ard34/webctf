# Default for local development (outside Docker)
NODE_ENV=development
PORT=3000

# Local MySQL (ubah sesuai environment lokalmu)
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=NewPassword123!
DB_NAME=ctf_platform

# JWT
JWT_SECRET=your-secret-key
JWT_EXPIRES_IN=7d

# Jika menjalankan via Docker Compose, backend akan override env melalui docker-compose.yml:
# DB_HOST=db
# DB_PORT=3306
# DB_USER=root
# DB_PASSWORD=rootpassword
# DB_NAME=ctf_platform